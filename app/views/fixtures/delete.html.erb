<%= link_to("<<back to list", {:action =>'list'}, :class => 'back-link') %>

<div class= "fixture delete">
  <h2>Delete fixture</h2>

  <%=form_for(:fixture, :url => {:action => 'destroy', :id => @fixtures}) do |f| %>

    <p>Are you sure want to permanently delete this fixture?</p>


      <div class= "form-buttons">
        <%= submit_tag("Delete fixture") %>
      </div>
  <% end %>


</div>